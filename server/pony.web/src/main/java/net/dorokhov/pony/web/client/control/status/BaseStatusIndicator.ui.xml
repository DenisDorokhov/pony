<ui:UiBinder xmlns:ui="urn:ui:com.google.gwt.uibinder"
             xmlns:g="urn:import:com.google.gwt.user.client.ui">

    <ui:style>
        .loadingIndicator {
            margin-top: 2em;
            text-align: center;
        }
        .image {
            width: 20px;
            height: 20px;
            margin-right: 10px;
        }
        .text {
            position: relative;
            top: 1px;
        }
    </ui:style>

    <g:FlowPanel addStyleNames="{style.loadingIndicator}">
        <g:Image ui:field="image" addStyleNames="{style.image}" />
        <g:InlineLabel ui:field="textLabel" addStyleNames="{style.text}" />
    </g:FlowPanel>

</ui:UiBinder>